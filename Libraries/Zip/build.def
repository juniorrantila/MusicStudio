#pragma once
#include "../../bs.h"

static auto const zip = cc_library("Zip", {
    .srcs = {
        "./Zip.c",
    },
    .exported_headers = {
        "./Zip.h",
    },
    .header_namespace = "Zip",
    .compile_flags = {},
    .linker_flags = {},
    .target_triple = {},
    .link_style = "static",
    .deps = {},
});
