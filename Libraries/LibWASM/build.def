#pragma once
#include "../../bs.h"
#include "../Basic/build.def"

static auto const wasm = cc_library("LibWASM", {
    .srcs = {
        "./VirtualMachine.c",
    },
    .exported_headers = {
        "Forward.h",
        "VirtualMachine.h",
    },
    .header_namespace = "LibWASM",
    .compile_flags = {},
    .linker_flags = {},
    .target_triple = {},
    .link_style = "static",
    .deps = {
        ty,
        basic,
    },
});
