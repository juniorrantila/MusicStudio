#pragma once
#include "../../../bs.h"

static auto const stb_image = cc_library("stb-image", {
    .srcs = {
        "./image.c",
    },
    .exported_headers = {
        "./image.h",
        "./stb_image.h",
    },
    .header_namespace = "stb",
    .compile_flags = {},
    .linker_flags = {},
    .target_triple = {},
    .link_style = "static",
    .deps = {},
});
