#pragma once
#include <Basic/Types.h>

typedef struct THThreadOSSpecific THThreadOSSpecific;
typedef struct THThread THThread;
typedef struct THSemaphore THSemaphore;
typedef struct THMessageQueue THMessageQueue;
typedef struct THMessage THMessage;
