#pragma once
#include "../../bs.h"
#include "../Basic/build.def"

#include "../../Vendor/stb/image/build.def"

static auto const image = cc_library("LibImage", {
    .srcs = {
        "./Image.cpp",
    },
    .exported_headers = {
        "./Image.h",
    },
    .header_namespace = "LibImage",
    .compile_flags = {},
    .linker_flags = {},
    .target_triple = {},
    .link_style = "static",
    .deps = {
        ty,
        basic,
        stb_image,
    },
});
